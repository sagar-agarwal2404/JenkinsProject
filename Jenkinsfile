pipeline {

    agent any

    stages {
        stage("build") {
           steps {
              echo 'buildng the app'
           }
        }

        stage("test") {
           steps {
               echo 'testing the app'
           }
        }

        stage("deploy") {
           steps {
               echo 'deploying the app'
          }
       }
    }
}
